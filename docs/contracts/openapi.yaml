openapi: 3.1.0
info:
  title: Potok API
  description: |
    Финансовый навигатор «Поток». Единый вход через api-gateway.
    Полная спецификация — docs/api/API_Contract.md
  version: 1.0.0
  contact:
    name: RK_J / Potok hackathon

servers:
  - url: http://localhost:8000/api/v1
    description: Local api-gateway

tags:
  - name: auth
  - name: dashboard
  - name: receipt
  - name: expenses
  - name: credits
  - name: analytics
  - name: ai
  - name: profile

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    Error:
      type: object
      properties:
        error:
          type: string

    RegisterRequest:
      type: object
      required: [phone]
      properties:
        phone:
          type: string
          example: "+79991234567"

    RegisterResponse:
      type: object
      properties:
        message:
          type: string
        expires_in:
          type: integer

    LoginRequest:
      type: object
      required: [phone, code]
      properties:
        phone:
          type: string
        code:
          type: string
          example: "0000"

    LoginResponse:
      type: object
      properties:
        access_token:
          type: string
        refresh_token:
          type: string
        expires_in:
          type: integer
        user:
          type: object
          properties:
            id:
              type: string
            phone:
              type: string
            role:
              type: string

    SankeyNode:
      type: object
      properties:
        name:
          type: string
        value:
          type: number

    SankeyLink:
      type: object
      properties:
        source:
          type: string
        target:
          type: string
        value:
          type: number

    SankeyResponse:
      type: object
      properties:
        nodes:
          type: array
          items:
            $ref: "#/components/schemas/SankeyNode"
        links:
          type: array
          items:
            $ref: "#/components/schemas/SankeyLink"

    ManualExpenseRequest:
      type: object
      required: [user_id]
      properties:
        user_id:
          type: string
        raw_text:
          type: string
          example: "купил продукты на 5000 и кроссовки за 16000"
        amount:
          type: number
        category:
          type: string
        description:
          type: string
        date:
          type: string
          format: date
        source:
          type: string
          enum: [manual, voice]
          default: manual

    ManualExpenseResponse:
      type: object
      properties:
        success:
          type: boolean
        id:
          type: string
        amount:
          type: number
        category:
          type: string
        parsed:
          type: boolean

    ReceiptManualRequest:
      type: object
      required: [store, amount, category, date]
      properties:
        store:
          type: string
        amount:
          type: number
        category:
          type: string
        date:
          type: string
          format: date-time

    ReceiptManualResponse:
      type: object
      properties:
        receipt_id:
          type: string
        store:
          type: string
        amount:
          type: number
        category:
          type: string
        date:
          type: string
        status:
          type: string

    ReceiptVoiceResponse:
      type: object
      properties:
        receipt_id:
          type: string
        store:
          type: string
        total:
          type: number
        category:
          type: string
        confidence:
          type: number

    ProfilePatchRequest:
      type: object
      properties:
        active_income:
          type: number
        passive_income:
          type: number
        emergency_fund:
          type: number
        fixed_expenses:
          type: array
          items:
            type: object
            properties:
              title:
                type: string
              amount:
                type: number
        goal_kind:
          type: string
        goal_title:
          type: string
        goal_amount:
          type: number
        skipped_income:
          type: boolean
        skipped_cushion:
          type: boolean
        skipped_goal:
          type: boolean
        skipped_expenses:
          type: boolean

    CreditScanResponse:
      type: object
      properties:
        parsed:
          type: object
          properties:
            amount:
              type: number
            rate:
              type: number
            term_months:
              type: integer
            monthly_payment:
              type: number
            bank:
              type: string
        benchmark_rate:
          type: number
        rate_vs_market:
          type: string
        confidence:
          type: number
        credit_id:
          type: string

    AiChatRequest:
      type: object
      required: [message]
      properties:
        message:
          type: string
        history:
          type: array
          items:
            type: object
            properties:
              role:
                type: string
              content:
                type: string

    AiChatResponse:
      type: object
      properties:
        reply:
          type: string

    CreditsDashboardResponse:
      type: object
      description: Front ожидает dti в процентах (0–100)
      properties:
        dti:
          type: number
          example: 38
        stress_test_dti:
          type: number
          example: 52
        monthly_income:
          type: number
        credits:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              name:
                type: string
              balance:
                type: number
              payment:
                type: number
              rate:
                type: number

    ScenarioSimulateRequest:
      type: object
      properties:
        scenario:
          type: string
          enum: [reduce_delivery, reduce_cafe, reduce_entertainment, custom]
        reduction_percent:
          type: number
        months:
          type: integer

    AiDiagnosisIndicator:
      type: object
      properties:
        name:
          type: string
        value:
          type: number
        norm:
          type: string
        status:
          type: string
          enum: [good, warning, critical]

    AiDiagnosisMainAction:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        potential_savings:
          type: number
        difficulty:
          type: string
          enum: [easy, medium, hard]

    AiDiagnosisResponse:
      type: object
      properties:
        score:
          type: number
          example: 72
        grade:
          type: string
          example: B
        indicators:
          type: array
          items:
            $ref: "#/components/schemas/AiDiagnosisIndicator"
        main_action:
          $ref: "#/components/schemas/AiDiagnosisMainAction"
        next_check_days:
          type: integer
          example: 30

paths:
  /auth/register:
    post:
      tags: [auth]
      summary: Регистрация по телефону
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterRequest"
      responses:
        "200":
          description: Пользователь зарегистрирован
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegisterResponse"
        "400":
          description: Неверный телефон
        "409":
          description: Пользователь существует

  /auth/login:
    post:
      tags: [auth]
      summary: Вход, JWT
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginResponse"
        "401":
          description: Неверный код

  /dashboard/sankey:
    get:
      tags: [dashboard]
      summary: Sankey доход → категории
      security:
        - bearerAuth: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SankeyResponse"
        "401":
          description: Unauthorized

  /dashboard/categories:
    get:
      tags: [dashboard]
      summary: Категории с детализацией
      security:
        - bearerAuth: []
      responses:
        "200":
          description: OK
        "401":
          description: Unauthorized

  /dashboard/timemachine:
    get:
      tags: [dashboard]
      summary: Траектория накоплений
      security:
        - bearerAuth: []
      responses:
        "200":
          description: OK
        "401":
          description: Unauthorized

  /expenses/manual:
    post:
      tags: [expenses]
      summary: Голос / ручной ввод расхода (legacy, §9)
      deprecated: true
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ManualExpenseRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManualExpenseResponse"
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /receipt/manual:
    post:
      tags: [receipt]
      summary: Ручной ввод расхода
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReceiptManualRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptManualResponse"
        "400":
          description: Bad request
        "401":
          description: Unauthorized

  /receipt/voice:
    post:
      tags: [receipt]
      summary: Голосовой ввод расхода (multipart audio)
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [audio]
              properties:
                audio:
                  type: string
                  format: binary
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptVoiceResponse"
        "400":
          description: Audio not recognized
        "401":
          description: Unauthorized

  /users/me/profile:
    patch:
      tags: [profile]
      summary: Обновить финансовый профиль (onboarding)
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProfilePatchRequest"
      responses:
        "200":
          description: Profile updated
        "401":
          description: Unauthorized
        "404":
          description: Not implemented yet

  /users/me/onboarding/complete:
    post:
      tags: [profile]
      summary: Завершить онбординг
      security:
        - bearerAuth: []
      responses:
        "200":
          description: OK
        "401":
          description: Unauthorized
        "404":
          description: Not implemented yet

  /ai/diagnosis:
    get:
      tags: [ai]
      summary: Финансовая картина пользователя
      security:
        - bearerAuth: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiDiagnosisResponse"
        "401":
          description: Unauthorized

  /ai/plan:
    get:
      tags: [ai]
      summary: Финансовый план и диагноз
      security:
        - bearerAuth: []
      responses:
        "200":
          description: OK
        "401":
          description: Unauthorized

  /ai/chat:
    post:
      tags: [ai]
      summary: Чат советника
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AiChatRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiChatResponse"
        "401":
          description: Unauthorized

  /credits/dashboard:
    get:
      tags: [credits]
      summary: Кредитный светофор, DTI
      security:
        - bearerAuth: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreditsDashboardResponse"
        "401":
          description: Unauthorized

  /credits/scan:
    post:
      tags: [credits]
      summary: Скан PDF кредитного договора
      security:
        - bearerAuth: []
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "200":
          description: Parsed contract
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreditScanResponse"
        "401":
          description: Unauthorized
        "422":
          description: Could not parse PDF

  /insights:
    get:
      tags: [analytics]
      summary: Поведенческие инсайты
      security:
        - bearerAuth: []
      responses:
        "200":
          description: OK

  /scenarios/simulate:
    post:
      tags: [analytics]
      summary: Симуляция «что если»
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScenarioSimulateRequest"
      responses:
        "200":
          description: OK

  /digest/latest:
    get:
      tags: [analytics]
      summary: Дайджест, действие недели
      security:
        - bearerAuth: []
      responses:
        "200":
          description: OK
