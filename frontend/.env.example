# Base URL API gateway (without /api/v1 suffix)
NUXT_PUBLIC_API_BASE=http://localhost:8000

# true  = офлайн-шорткаты (mock JWT, localStorage-профиль, без advisor API)
# false = реальный бэкенд: логин код 0000 → JWT → /ai/*, /dashboard/*
NUXT_PUBLIC_DEMO_MODE=false
